// Costanti
const nome = "Alice";
const colorePreferito = "Blu";

// Variabili che possono cambiare
let eta = 42;
let citta = "Roma";

// Stampa iniziale
console.log(`Mi chiamo ${nome}, ho ${eta} anni e vivo a ${citta}. Il mio colore preferito è ${colorePreferito}.`);

// Simuliamo un cambiamento
eta = 43;
citta = "Milano";

console.log(`Ho cambiato città! Ora vivo a ${citta} e ho ${eta} anni.`);
